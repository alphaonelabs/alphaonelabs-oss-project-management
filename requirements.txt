httpx==0.27.0
pyjwt==2.8.0
cryptography==46.0.5
python-dotenv==1.0.0

# Note: These dependencies are for reference only.
# Python Cloudflare Workers use their own runtime
# and access browser APIs via the 'js' module.
# No pip install is needed for deployment.
#
# Security: cryptography updated to 46.0.5 to address:
# - CVE: Subgroup Attack for SECT Curves
# - CVE: NULL pointer dereference in pkcs12.serialize_key_and_certificates
# - CVE: Bleichenbacher timing oracle attack
